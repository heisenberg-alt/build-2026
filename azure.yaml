# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: build-2026-deck-proxy
metadata:
  template: build-2026-deck-proxy@1.0.0
services:
  api:
    project: api
    language: js
    host: function
